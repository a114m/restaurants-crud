# Restaurants CRUD Example

An example of RESTful API that allows CRUD operations using PlayFramework

### Running the server

Run this using sbt

```bash
sbt run
```

The end-point supported is: <http://localhost:9000/api/restaurant>
